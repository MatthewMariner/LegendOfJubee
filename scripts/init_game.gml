///initialize game

//Global Variable boolean
global.gamepad = gamepad_is_connected(0);

//Where the player.sprite is facing
enum enum_facing {
    up = 90,
    right = 0,
    down = 270,
    left = 180
}
